{
  "provider": {
    "type": "string",
    "_special": "select_provider",
    "description": "用于生成日记的模型提供商（留空则使用当前会话默认模型）",
    "default": ""
  },
  "interval_days": {
    "description": "每隔多少天自动总结一次",
    "type": "int",
    "default": 1,
    "hint": "设置定时自动总结的间隔天数，最小为 1 天"
  },
  "max_messages": {
    "description": "每次总结最近多少条聊天记录",
    "type": "int",
    "default": 50,
    "hint": "控制每次送入大模型总结的聊天记录条数上限"
  },
  "custom_prompt": {
    "description": "自定义日记生成提示词（可选）",
    "type": "text",
    "default": "",
    "hint": "留空使用默认风格。填写后会追加到系统提示词中，用于微调日记的写作风格"
  }
}
